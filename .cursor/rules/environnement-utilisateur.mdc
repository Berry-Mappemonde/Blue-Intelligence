---
description: User environment (local macOS machine, beginner level, OVH VPS)
alwaysApply: true
---

# User environment

- The user's local machine is a **Mac (macOS)**: any command or instruction
  the user must run must be given for macOS (Terminal, `cat`, `pbcopy`,
  `ssh-copy-id`…), never for Windows.
- The user is a **beginner**: prefer step-by-step instructions, explain
  non-trivial commands, avoid undefined jargon.
- Production server: **OVH Ubuntu VPS** `vps-52ba6d62.vps.ovh.net`
  (135.125.226.16), 4 vCores / 8 GB RAM / 75 GB — nginx in front, domain
  `blueintelligence.online` behind Cloudflare.

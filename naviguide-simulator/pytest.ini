[pytest]
pythonpath = server
testpaths = server/tests
# A test that hangs longer than this dumps every thread's stack to stderr
# (the run goes on). In CI that trace is the only clue left after a freeze.
faulthandler_timeout = 90
